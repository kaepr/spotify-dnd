import React, { useEffect, useState } from 'react';

const SpotifyPlaylist = (props) => {
  // console.log('props = ', props.data);

  return <div className="flex-grow border-r-4 h-screen">Spotify Playlist</div>;
};

export default SpotifyPlaylist;
