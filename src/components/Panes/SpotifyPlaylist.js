import React, { useEffect, useState } from 'react';

const SpotifyPlaylist = (props) => {
  console.log('props = ', props.data);

  return <div className="flex-grow">Spotify Playlist</div>;
};

export default SpotifyPlaylist;
